using System;
using System.IO;

namespace Week1Task
{
    class renameDirectory
    {
        /*public static void Main(string[] args)
        {
            Console.Write("Enter the path of the directory you want to rename: ");
                string sourcePath = Console.ReadLine();
                Console.Write("Enter the new name of the directory: ");
                string targetPath = Console.ReadLine();
                RenameDirectory(sourcePath, targetPath);
        }

        static void RenameDirectory(string sourcePath, string targetPath)
        {
            if (Directory.Exists(sourcePath))
            {
                try
                {
                    Directory.Move(sourcePath, targetPath);
                    Console.WriteLine("Directory successfully renamed");
                }
                catch (IOException e)
                {
                    Console.WriteLine("Error while renaming directory: " + e.Message);
                }
            }
            else
            {
                Console.WriteLine("Directory does not exist");
            }
        }*/
    }
}

